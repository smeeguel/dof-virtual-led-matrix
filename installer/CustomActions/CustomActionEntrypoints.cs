namespace VirtualDofMatrix.CustomActions;

public static class CustomActionEntrypoints
{
    // This placeholder is intentionally simple: the installer transition introduces a dedicated
    // elevated custom-action assembly now, and concrete elevated entry points can be added next.
    public static int ValidateElevatedContext() => 0;
}
