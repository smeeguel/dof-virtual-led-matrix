# Signing Assets (Placeholders)

This folder intentionally contains placeholders only.

## Subfolders

- `ev-cert/` - EV certificate process notes (no private keys committed).
- `attestation/` - Microsoft attestation submission artifacts template.

## Security

Never commit:
- PFX/private key material
- Hardware token exports
- Secrets, passwords, or tenant credentials
